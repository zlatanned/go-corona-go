const chalk = require('chalk');

module.exports = async function forecast() {
    console.log("STAY HOME, STAY SAFE");
    console.log(
        `${chalk.greenBright(
            "WE'LL DEFEAT THIS !"
        )}`
    );
}
